import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        position: 'absolute',
        top: 65,
        right: 30,
        backgroundColor: '#fff',
        width: '40%',
        height: '5%',
        borderRadius: 8,
        padding: 8
    },
    buttonLogout: {},
    textButtonLogout: {
        fontSize: 16,
        color: 'red',
        fontWeight: '600'
    },

})

export default styles