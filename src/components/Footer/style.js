import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    footer: {
        borderColor: 'gray',
        alignItems: 'center',
        padding: 5,        
        position: 'absolute',
        bottom: 5
        
    },
    footerText: {
        fontWeight:'400',
        fontSize: 12
    }
})

export default styles