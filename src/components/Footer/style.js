import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    footer: {
        borderTopWidth: 1,
        borderColor: 'gray',
        alignItems: 'center',
        padding: 5,        
    },
    footerText: {
        fontWeight:'500',
        fontSize: 12
    }
})

export default styles